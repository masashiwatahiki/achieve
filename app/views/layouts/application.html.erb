<!DOCTYPE html>
<html>
<head>
  <title>例コード</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <header>
    <div class="title">
      Rails Blog Sample
    </div>
    <div class="login">
      <%= link_to 'Contact', new_contact_path, class: 'btn btn-default' %>
      <% flash.each do |key, value| %>
        <%= content_tag(:div, value, class: "#{key}") %>
      <% end %>

      <% if logged_in? %>
        <%= link_to "Profile", user_path(current_user.id) ,class: 'btn btn-default'%>
        <%= link_to "Logout", session_path(current_user.id), method: :delete,class: 'btn btn-default' %>
      <% else %>
        <%= link_to "Sign up", new_user_path ,class: 'btn btn-default' %>
        <%= link_to "Login", new_session_path ,class: 'btn btn-default'%>
      <% end %>
    </div>
  </header>
  <div class ="content">
    <%= yield %>
  <footer>
    <p>© Watahiki Masashi</p>
  </footer>

</body>
</html>
